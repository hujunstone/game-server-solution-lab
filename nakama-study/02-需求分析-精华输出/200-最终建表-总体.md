角色 背景 出身 势力
动态属性 技能 装备
商队

- 角色创建：`character` + `background` + `faction`
- 身份与声望：`character.reputation_score` + `reputation_level`
- 属性系统：`character_attribute`
- 技能系统：`skills` + `character_skills`
- 装备系统：`equip_slots` + `item_rarities` + `equipments` + `character_equipments`
- 商队系统：`caravan_roles` + `caravan_npcs` + `character_caravan_members` (+ `background_initial_members`)

todo
1. 建表应该用 varchar 还是 string 是否应该限制字符串的长度


