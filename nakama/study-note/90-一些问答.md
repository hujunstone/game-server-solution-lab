

3. Nakama 的配置文件在哪，一些变量的配置在哪改，
4. 一般这样的go语言工程用户自定义的一些变量需要存到配置文件中，如何读写，变量修改后如何在不重启状态下，快速生效，要求回答言简意赅，通俗易懂，重要信息不丢失
1. nakama 的建表语句在哪
2. cockroachdb 如何查询具体表里面的数据

select * from public.console_user


宿主机执行
cockroach sql --insecure --host=localhost:26257
```sql
SHOW DATABASES;
USE mydb;

SHOW TABLES;
SHOW COLUMNS FROM users;

-- 查前 20 条记录
SELECT * FROM users LIMIT 20;
## cockroach 客户端不支持 \G PG客户端支持
SELECT * FROM users WHERE id = 1\G

-- 带条件查询
SELECT * FROM users WHERE id = 1;

-- 只查部分字段
SELECT name, email FROM users LIMIT 10;

```

# 或者用pg的client
psql "postgresql://root@localhost:26257/defaultdb?sslmode=disable"
```sql
use nakama;
select * from console_user\G
```
输入竖排模式开启命令（注意，这一行只有 \x on）：
\x on
如果你想恢复横向表格显示：
\x off
